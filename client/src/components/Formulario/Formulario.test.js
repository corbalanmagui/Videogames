import React from "react";
import { render } from "@testing-library/react";
import Formulario from "./Formulario";

// describe('<Formulario /> Mounted', () => {
//   let wrapper;
//   beforeEach(() => {
//     wrapper = mount(<Formulario />);
//   })
// })
